# kubectl create secret generic netbox-rag-secrets \
#   --namespace netbox \
#   --from-literal=OPENAI_API_KEY=sk-... \
#   --from-literal=ASSISTANT_ID=asst_... \
#   --dry-run=client -o yaml | kubectl apply -f -
#
# Do NOT commit real keys here. Apply the secret manually once.
apiVersion: v1
kind: Secret
metadata:
  name: netbox-rag-secrets
  namespace: netbox
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_ME"
  ASSISTANT_ID: "REPLACE_ME"
