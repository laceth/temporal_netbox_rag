FROM python:3.11-slim
WORKDIR /app
COPY workspace/server/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY workspace/server/ ./server/
ENV PYTHONPATH=/app
EXPOSE 8000
CMD ["uvicorn", "server.main:app", "--host", "0.0.0.0", "--port", "8000"]
